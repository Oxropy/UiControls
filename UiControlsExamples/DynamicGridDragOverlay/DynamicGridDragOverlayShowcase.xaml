<Window x:Class="UiControlsExamples.DynamicGridDragOverlay.DynamicGridDragOverlayShowcase"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:dynamicGridDragOverlay="clr-namespace:UiControlsExamples.DynamicGridDragOverlay"
        xmlns:view="clr-namespace:UiControls.DynamicGrid.View;assembly=UiControls"
        xmlns:view1="clr-namespace:UiControls.DropOverlay.View;assembly=UiControls"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance dynamicGridDragOverlay:DynamicGridDragOverlayViewModel}"
        Title="DynamicGridDragOverlayShowcase"
        Height="450"
        Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0"
                    Margin="5">
            <Button Content="Reset"
                    Command="{Binding ResetCommand}" />
            <StackPanel Orientation="Horizontal">
                <TextBox Text="{Binding XPosition}" />
                <TextBox Text="{Binding YPosition}" />
                <TextBox Text="{Binding ColorHash}" />
            </StackPanel>
            <Button Content="Add"
                    Command="{Binding AddCommand}" />
            <Button Content="Remove"
                    Command="{Binding RemoveCommand}" />

            <TreeView x:Name="SourceTreeView">
                <TreeViewItem Header="#FF0000FF" />
                <TreeViewItem Header="#00FF00FF" />
                <TreeViewItem Header="#0000FFFF" />
            </TreeView>
        </StackPanel>

        <view:DynamicGrid Grid.Column="1"
                                 Name="DynamicGrid"
                                 Margin="5"
                                 ContextMenuItem="{Binding DynamicGridManager.ContextMenuStart}">
            <b:Interaction.Triggers>
                <b:EventTrigger EventName="OpenContextMenu">
                    <b:InvokeCommandAction Command="{Binding DynamicGridManager.CreateContextMenuCommand}"
                                           PassEventArgsToCommand="True"/>
                </b:EventTrigger>
            </b:Interaction.Triggers>
        </view:DynamicGrid>

        <view1:DropOverlayControl Grid.Column="1"
                                        x:Name="DropOverlay"
                                        Width="{Binding ActualWidth, ElementName=DynamicGrid}"
                                        Height="{Binding ActualHeight, ElementName=DynamicGrid}"
                                        IsHitTestVisible="False">
            <view1:DropOverlayControl.Templates>
                <view1:DropOverlayTemplate Position="Top">
                    <Image Source="/DragOverlay/Images/add_above.png"
                           Height="20"
                           Width="20" />
                </view1:DropOverlayTemplate>
                <view1:DropOverlayTemplate Position="Right">
                    <Image Source="/DragOverlay/Images/add_above.png"
                           Height="20"
                           Width="20">
                        <Image.LayoutTransform>
                            <RotateTransform Angle="90" />
                        </Image.LayoutTransform>
                    </Image>
                </view1:DropOverlayTemplate>
                <view1:DropOverlayTemplate Position="Bottom">
                    <Image Source="/DragOverlay/Images/add_above.png"
                           Height="20"
                           Width="20">
                        <Image.LayoutTransform>
                            <RotateTransform Angle="180" />
                        </Image.LayoutTransform>
                    </Image>
                </view1:DropOverlayTemplate>
                <view1:DropOverlayTemplate Position="Left">
                    <Image Source="/DragOverlay/Images/add_above.png"
                           Height="20"
                           Width="20">
                        <Image.LayoutTransform>
                            <RotateTransform Angle="270" />
                        </Image.LayoutTransform>
                    </Image>
                </view1:DropOverlayTemplate>
                <view1:DropOverlayTemplate Position="Center">
                    <Border Background="#80FF00FF"
                            CornerRadius="5">
                        <TextBlock Text="Drop Here"
                                   HorizontalAlignment="Center" />
                    </Border>
                </view1:DropOverlayTemplate>
            </view1:DropOverlayControl.Templates>
        </view1:DropOverlayControl>
    </Grid>
</Window>